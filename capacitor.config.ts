import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.organizationlist',
  appName: 'Organization-List',
  webDir: 'dist'
};

export default config;
